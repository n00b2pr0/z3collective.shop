Text input with label/hint/error. Focus = sticker shadow pop.

```jsx
<Input label="Email" placeholder="you@example.com" hint="For order updates only." />
<Input label="Name" error="We need this one." />
```

Also exports `Field` — the label/hint wrapper, reused by Select.
