Round icon-only button (44px default hit target). Pass a Lucide SVG as children; `label` is required.

```jsx
<IconButton label="Cart"><svg …lucide shopping-cart…/></IconButton>
<IconButton quiet size="sm" label="Close">✕-style Lucide x icon</IconButton>
```
