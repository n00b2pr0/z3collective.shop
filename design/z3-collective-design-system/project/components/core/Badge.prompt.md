Uppercase pill badge for line labels and statuses ("NEW", "DRAGONS", "SOLD OUT").

```jsx
<Badge line="dragons">Dragons</Badge>
<Badge ink>Sold out</Badge>
<Badge>New</Badge>
```

Default fill is teal; `line` uses that product line's accent. Candy and Yarn are locked and intentionally not offered.
