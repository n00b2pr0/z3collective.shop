The signature Z3 surface: white sticker card with 2px ink border and hard offset shadow.

```jsx
<Card>Any content</Card>
<Card interactive padded={false} onClick={open}>{/* full-bleed image + caption */}</Card>
<Card quiet>Low-emphasis container</Card>
```
