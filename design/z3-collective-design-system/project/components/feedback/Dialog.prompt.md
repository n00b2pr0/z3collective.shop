Modal dialog — sticker card over an ink scrim.

```jsx
<Dialog open={open} title="Remove from cart?" onClose={close}
  actions={<><Button variant="ghost" onClick={close}>Keep it</Button><Button onClick={confirm}>Remove</Button></>}>
  The Flexi Dragon goes back on the shelf.
</Dialog>
```
