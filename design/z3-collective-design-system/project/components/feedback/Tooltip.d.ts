/** Small ink tooltip above the wrapped element on hover/focus. */
export interface TooltipProps {
  text: string;
  children?: React.ReactNode;
}
export declare function Tooltip(props: TooltipProps): JSX.Element;
