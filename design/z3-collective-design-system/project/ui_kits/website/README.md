# Z3 Collective — Website UI kit

**No product source existed** (no codebase, no Figma, no screenshots). This kit is *derived from the brand brief*, not a recreation — flagged accordingly. Replace with recreations if real site sources arrive.

Screens (click-through in `index.html`):
- **Home** — hero, product-line grid, featured products
- **Line page** — Dragons line with accent header, filters, product grid
- **Product page** — Flexi Dragon detail: size, gift wrap, add-to-cart with toast

All product imagery is placeholder blocks — no real photos were provided. Components come from `components/` via the compiled bundle; nothing is re-implemented here.
